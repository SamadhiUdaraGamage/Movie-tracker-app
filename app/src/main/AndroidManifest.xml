<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mobilecoursework02">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MobileCoursework02">
        <activity android:name=".TotalRating"></activity>
        <activity android:name=".imdb" />
        <activity android:name=".Edit" />
        <activity android:name=".row_movie_edit" />
        <activity android:name=".row_movie" />
        <activity android:name=".Ratings" />
        <activity android:name=".Search" />
        <activity android:name=".Edit_Movies" />
        <activity android:name=".Favourites" />
        <activity android:name=".Display_Movies" />
        <activity android:name=".Register_Movie" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>